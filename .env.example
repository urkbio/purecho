# 站点配置
SITE_URL="http://example.com"  # 替换为你的实际域名

# Flask配置
FLASK_ENV="production"  # 生产环境设置为 production
SECRET_KEY="your-secret-key-here"  # 替换为随机生成的密钥，可以使用 python3 -c 'import secrets; print(secrets.token_hex())' 生成